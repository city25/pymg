"""Downloader helper (stub)."""
from typing import Optional


def download(url: str, dest: str, timeout: Optional[int] = None) -> str:
    """Download resource from `url` to `dest`. Returns path to file."""
    return dest
